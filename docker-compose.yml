version: '3.7'

services:
  db:
    image: postgres
    restart: always
    ports:
    - "5432:5432"
    environment:
      POSTGRES_USER: user_example
      POSTGRES_PASSWORD: example
      POSTGRES_DB: report_database